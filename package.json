{
  "license": "MIT",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "lint": "prettier --write 'src/**/*.ts'",
    "test": "npm run build && node --test dist/tests/filter.test.js"
  },
  "dependencies": {
    "pandoc-filter": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "prettier": "^3.3.1",
    "typescript": "^5.4.5"
  }
}
