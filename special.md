# 特殊仕様について

## HTML コードブロック

`<pre><code class="html"></code></pre>` タグを使用して HTML コードブロックを作成することができますが、Redmine では `<pre>` を含む場合において正しく動作しないことがあります。

```
<pre><code class="html">
<pre>
preformatted
</pre>
</code></pre>
```

コードブロック中の `<pre>` は HTML の特殊文字に変換され `&lt;pre&gt;` のように置き換えられますが、`&lt;` がそのまま表示されてしまい、結果、本来意図した通りに表示されません。
言語指定を行わないコードブロックであれば、特殊文字の変換は行われないため、問題を回避することはできます。
